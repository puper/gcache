# gcache v2 文档索引

## 项目阶段
- 阶段：性能优化与可构建性修复（2026-04-22）
- 核心目标：恢复 `v2` 可编译可测试，并完成分片、哈希、TTL 清理路径的性能优化

## 文档路由表
- 需求与优化范围：`docs/requirements.md`
- 架构与并发设计：`docs/architecture.md`
- 数据契约与接口变化：`docs/schema.md`
- 开发与验证手册：`docs/dev_manual.md`
- **代码审查报告（最终）**：`docs/CODE_REVIEW_FINAL.md`（2026-04-23）

## 关键变更日志（Changelog）
- 2026-04-22：新增 `v2` 文档体系，建立 DDD 索引。
- 2026-04-22：规划修复缺失的 `shardedCache/newShardedCache` 实现，补齐核心缓存能力。
- 2026-04-22：规划 TTL 从"全量扫描 map"切换到"每分片最小堆到期队列"，降低清理复杂度与锁竞争。
- 2026-04-22：规划哈希策略升级（字符串 `maphash` + 数值混洗 + 泛型兜底），并支持可配置分片数量。
- 2026-04-22：完成核心实现与性能优化落地，`go test ./...` 与 `go test -bench=. -benchmem -benchtime=200ms -run=^$ ./...` 均通过。
- 2026-04-22：参考 `tasktimer` 将 TTL 从最小堆切换为单层时间轮（slot 扫描），接受 `resolution` 粒度误差以换取更高吞吐。
- 2026-04-22：规划时间轮升级为"24h 固定窗口环形槽数组 + 长 TTL 自动续挂"，继续保持 `Resolution` 可配置。
- 2026-04-22：完成环形槽升级（24h 固定窗口 + 长 TTL 自动续挂），新增长 TTL 回归测试并通过基准验证。
- 2026-04-22：完成双容量模式支持（有限容量 + 无限容量），修复无限容量下分片计算退化问题并补齐回归测试。
- 2026-04-22：完成稳定性修复：复杂 key 兜底哈希改为 `maphash.Comparable`、`onEvict` panic 隔离、32 位索引与扩容边界修正，并通过 `linux/386` 交叉编译检查。
- 2026-04-22：明确版本发布规范：`v2` 目录作为独立 Go Module，新增 `go.mod` 并使用 `github.com/puper/gcache/v2` 模块路径。
- 2026-04-23：完成第一轮代码审查，发现 11 个问题（P0:5, P1:2, P2:2, P3:2）。
- 2026-04-23：完成第二轮代码审查，修复所有 P0/P1 问题，代码质量显著提升。
- 2026-04-23：完成第三轮代码审查（最终），通过！剩余 6 个可选优化项（非阻塞）。
- 2026-04-23：完成审查问题首批修复：新增 `ErrCapacityExceeded`、修复时间轮负索引与回拨推进、修正容量淘汰过期原因误报、补齐 `ringList` 索引防御与回归测试。
- 2026-04-23：完成第二轮问题修复：降低 `popExpired` 大跨度扫描持锁风险、统一过期判定时间基准、`SetWithExpire` 改为锁内时间戳采样，并补充回归测试。
- 2026-04-23：完成 P1/P2 收尾修复：修复 `floorPowerOfTwo` 极大输入溢出死循环风险，为时间轮 `Resolution` 增加槽数上限归一化，并新增回归测试。
